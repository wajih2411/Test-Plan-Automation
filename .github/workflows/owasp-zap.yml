name: OWASP ZAP Baseline Scan

# This workflow will trigger on a push to any branch and on pull requests to main
on:
  push:
    branches:
      - '**'  # Trigger for pushes to all branches
  pull_request:
    branches:
      - main  # Trigger for pull requests to main

jobs:
  zap_scan:
    runs-on: ubuntu-latest  # Use the latest Ubuntu runner

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '14'

    - name: Install dependencies
      run: npm install

    - name: Run ZAP Baseline Scan
      run: |
        const Octokit = require("@octokit/rest");
        const octokit = new Octokit({
          auth: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJfaWQiOiI2NWM2OTcxNmE2OGQ4MzAwMTMyNTg0OTQiLCJpYXQiOjE3MTEzMTYwMjJ9.tLs2z9hMybMoOageR_MJrO5JJf3isM7DMesLqfEklws",
        });

        const { spawnSync } = require('child_process');

        console.log('Alerts present in the current report:', process.env.ZAP_ALERTS_PRESENT);

        if (process.env.ZAP_ALERTS_PRESENT === 'true') {
          console.log('ZAP Scan detected alerts. Failing the workflow.');
          process.exit(1);
        } else {
          console.log('ZAP Scan did not detect any alerts. Workflow continues.');
        }
      env:
        ZAP_ALERTS_PRESENT: ${{ env.ZAP_ALERTS_PRESENT }}
